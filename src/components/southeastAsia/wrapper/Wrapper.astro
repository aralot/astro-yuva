---
import Layout from '../../../layouts/Layout.astro';
---

<Layout title="Jawablah beberapa pertanyaan dan dapatkan kelas coding gratis untuk anak Anda">
  <div class="container">
    <div class="content">
      <div class="logo"></div>
      <div class="about">
        <slot />
      </div>
    </div>
  </div>
</Layout>

<style>
  .container {
    position: relative;
    height: 100%;

    background: linear-gradient(
      0deg,
      rgba(230, 230, 230, 0.5),
      rgba(230, 230, 230, 0.5)
    ),
    linear-gradient(0deg, rgba(250, 243, 255, 0.2), rgba(250, 243, 255, 0.2)),
    #ffffff;

    @media (min-width: 75em) {
      background: linear-gradient(
        0deg,
        rgba(230, 230, 230, 0.5),
        rgba(230, 230, 230, 0.5)
      ), rgba(250, 243, 255, 0.2);

      &:before {
        content: '';
        display: block;
        width: 100%;
        height: 100%;

        position: absolute;
        right: 0;
        bottom: 0;

        background: url(/background.webp) no-repeat;
        background-position: bottom right;
        background-size: cover;

        pointer-events: none;
      }
    }
  }

  .content {
    position: relative;
    display: flex;
    flex-direction: column;
    height: 100%;

    @media (min-width: 75em) {
      width: 1160px;
      margin: 0px auto;
    }
  }

  .logo {
    height: 58px;
    color: #602b7a;
    padding-top: 25px;
    padding-left: 20px;

    @media (min-width: 75em) {
      position: relative;
      margin-right: 113px;
      height: 33px;
      color: white;
      padding-left: 4px;

      &:before {
        content: "";
        display: block;
        width: 173px;
        height: 215px;
        position: absolute;
        bottom: -75px;
        left: 5px;
        background: rgb(96, 43, 122);
        transform: rotate(34.34deg);
      }

      &:after {
        content: "";
        display: block;
        width: 132px;
        height: 33px;
        background: url(/logo.svg) no-repeat;
        position: relative;
        top: 25px;
      }
    }

  }

  .about {
    margin: auto 0px;
    max-width: 364px;
    padding: 162px 0px 0px;
  }

  .title {
    margin-bottom: 12px;
    width: 517px;
    font-size: 48px;
    line-height: 52px;
    font-weight: 700;
  }

  .description {
    padding-bottom: 24px;
  }

  p {
    margin: 0px;
    color: rgb(15, 28, 44);
    font-family: "Cera CY", Helvetica, Arial, sans-serif;
    font-size: 18px;
    line-height: 24px;
    padding-bottom: 8px;
  }
</style>
